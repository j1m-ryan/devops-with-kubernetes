apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim
spec:
  storageClassName: manual
  resources:
    requests:
      storage: 1Gi 
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
