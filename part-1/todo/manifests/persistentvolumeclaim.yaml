apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: image-claim 
spec:
  resources:
    requests:
      storage: 1Gi
  volumeMode: Filesystem
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
